nodeLinker: node-modules

enableGlobalCache: false

# Allow peer dependency warnings but don't fail
peerDependencyRules:
    allowAny:
        - "react"
        - "react-dom"

# Disable immutable installs (allow lockfile updates locally)
enableImmutableInstalls: false

# Network settings
httpTimeout: 60000
networkConcurrency: 8
