nodeLinker: node-modules

enableGlobalCache: false

# Disable immutable installs (allow lockfile updates locally)
enableImmutableInstalls: false

# Network settings
httpTimeout: 60000
networkConcurrency: 8
