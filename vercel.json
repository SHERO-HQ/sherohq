{
  "framework": "nextjs",
  "buildCommand": "corepack enable && corepack prepare yarn@4.12.0 --activate && yarn build",
  "installCommand": "corepack enable && corepack prepare yarn@4.12.0 --activate && yarn install",
  "outputDirectory": ".next",
  "rewrites": [
    {
      "source": "/og/:id",
      "destination": "/api/og/:id"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/daily",
      "schedule": "0 2 * * *"
    }
  ]
}